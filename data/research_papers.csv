import pandas as pd

# File path for the CSV where the research papers will be saved
DATA_FILE = 'research_papers.csv'  # Adjust if needed

# Function to load data from the CSV file
def load_data():
    try:
        df = pd.read_csv(DATA_FILE)
        print("Data loaded successfully")
        print(df.head())  # Display the first few rows for inspection
        return df
    except FileNotFoundError:
        print("No file found, returning empty DataFrame.")
        return pd.DataFrame(columns=['Title', 'Author', 'University', 'Year', 'Category', 'Link', 'PDF'])
